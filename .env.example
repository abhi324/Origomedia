# ─── Origometer wiring ────────────────────────────────────────────────────────
# URL of the FastAPI backend (Render in prod, localhost in dev).
# Used by next.config.js to rewrite /api/v1/* → backend.
ORIGOMETER_BACKEND_URL=https://origo-backend.onrender.com
